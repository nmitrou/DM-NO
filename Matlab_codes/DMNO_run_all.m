%% concatenate data
    % BP
    % RBF
    % LS
    % transfer functions
    % LS transfer functions
    % PC means
    % PD means
    % Cluster numbers
%% load concatenated data
%% generate plots
    % BP, RBF, LS
    % transfer functions
    % Cluster imagte
        % Cluster map, PC map, PD map
%% export plots